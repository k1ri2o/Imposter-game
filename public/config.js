// Configuration file for Socket.IO server URL
// You can set this via environment variable during build, or update manually
// For local development, this will be ignored and client.js will use localhost
window.ENV = window.ENV || {};
// Set your production server URL here if not using environment variables
window.ENV.SOCKET_SERVER_URL = window.ENV.SOCKET_SERVER_URL || '';

